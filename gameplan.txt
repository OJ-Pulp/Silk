Show entities
1.) "Node Entity:" and "Edge Entity:" both are dropdowns and both will show the value in the graph

Edit toolbar
1.) Each Tool Except Filter has "[Tool]:" Drop down of different methods for that method. Must call backend for list of function names for that specific tool
2.) Must save it to a user file on the backend

Create Entity
1.) Entity Name
2.) Entity Type (Text, Integer, Decimal)

Create Node
0.) Node ID -> user input but must check backend
1.) Select from List of Entities (Call to backend for entity list and Entity Types For nodes specifically) (Drop Down)
2.) Option to keep adding Entities

Create Edge
0.) Node #1 and Node #2 with 2 drop downs, these values do not have to be called from the backend but can instead be grabbed from data.js
1.) Select from List of Entities (Call to backend for entity list and Entity Types For Edges specifically) (Drop Down)
2.) Option to keep adding Entities

Create Graph
1.) Have user put in graph ID "Graph ID:", check if id is available in graphs

**Allow User to Input in ID for graphs and nodes are generated by the User, if no ID is provided then use a UUID**
**graph jsons should have graph ID in them along with node and edge data**
**User file will need to have the path to the graph or display a blank one if it cant find it**



Filter Graph: Note that filter graph will allow us to show multiple graphs at the same time. So we can just Compare graphs/Clusters after showing 2 graphs or more

1.) Graphs: Drop down with plus to allow for more than 1
2.) Nodes: if none show all, otherwise dropdown with entity names and then a text input area for each, also a plus to add more than one
3.) Edges: Same as nodes but for edges



